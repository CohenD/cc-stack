# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env / secrets — never commit real keys
.env
.env*.local

# duckdb local databases (keep the data/ dir via .gitkeep)
data/*.duckdb
data/*.duckdb.wal
data/*.db
*.duckdb
*.duckdb.wal

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
*.tsbuildinfo
